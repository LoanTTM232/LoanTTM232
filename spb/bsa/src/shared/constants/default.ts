export default {
  // default port
  PORT: 3000,
  // default timestamp format
  TIMESTAMP_FORMAT: 'YYYY-MM-DD HH:mm:ss',
  // default sort limit
  SORT_LIMIT: 50,
  // default sort page
  SORT_PAGE: 1,
  // default cookie max age (10 minutes)
  REFRESH_TOKEN: 'refresh',
  ACCESS_TOKEN: 'access'
}
