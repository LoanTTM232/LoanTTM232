import React, { FC } from 'react';
import { Text, View } from 'react-native';

const TimeSelection: FC = () => {
  return (
    <View>
      <Text>Time Selection Component</Text>
    </View>
  );
};

export default TimeSelection;
